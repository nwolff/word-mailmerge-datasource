[tool.poetry]
name = "word-mailmerge-datasource"
version = "0.1.0"
description = ""
authors = ["nwolff"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
bottle = "^0.12.23"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
